/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'res.cloudinary.com',
      // domains อื่นๆ ที่ต้องการ
    ],
  },
  // ... config อื่นๆ
};

export default nextConfig;
